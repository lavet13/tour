FROM node:20-alpine AS builder

WORKDIR /app

COPY package.json yarn.lock ./

RUN yarn install --frozen-lockfile

COPY . .

RUN yarn build

# The built files are in /app/dist and will be mounted as a volume
# Keep the container running so the volume persists
CMD ["tail", "-f", "/dev/null"]
